name: T006 Closeout Final Exact Head

on:
  push:
    branches:
      - validation/t006-closeout-1a1a5b0e

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact candidate
        uses: actions/checkout@v4
        with:
          ref: 1a1a5b0e15fbc120f95dfe37b3c66b4d2391175d
          fetch-depth: 1

      - name: Verify exact HEAD
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "1a1a5b0e15fbc120f95dfe37b3c66b4d2391175d"
          git rev-parse HEAD

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: npm ci
        run: npm ci

      - name: Build bootstrap
        run: npm run build

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Full test suite
        run: npm test

      - name: T-006 focused predicate and XState tests
        run: node --import tsx --test "packages/domain-harness/tests/workflow-v03/predicate-purity.test.ts" "packages/domain-harness/tests/workflow-v03/xstate-boundary.test.ts"

      - name: Package smoke
        run: npm pack -w @kaicreator/domain-harness
