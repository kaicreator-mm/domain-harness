name: T006 Closeout Exact Head Final

on:
  push:
    branches:
      - validation/t006-closeout-7b80172b

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact candidate
        uses: actions/checkout@v4
        with:
          ref: 7b80172b92eb0fd75c95baa3561536ea5c9508c6
          fetch-depth: 1

      - name: Verify exact HEAD
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "7b80172b92eb0fd75c95baa3561536ea5c9508c6"
          git rev-parse HEAD

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: npm ci
        run: npm ci

      - name: Build bootstrap
        run: npm run build

      - name: Lint after build bootstrap
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Full test suite
        run: npm test

      - name: T-006 focused predicate and XState tests
        run: node --import tsx --test "packages/domain-harness/tests/workflow-v03/predicate-purity.test.ts" "packages/domain-harness/tests/workflow-v03/xstate-boundary.test.ts"

      - name: Package smoke
        run: npm pack -w @kaicreator/domain-harness
