{
  "name": "@kaicreator/domain-harness-expo",
  "version": "0.2.0",
  "description": "React Native / Expo host bindings for DomainHarness",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "prebuild": "npm run build -w @kaicreator/domain-harness",
    "build": "tsc -p tsconfig.build.json",
    "pretypecheck": "npm run build -w @kaicreator/domain-harness",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run typecheck"
  }
}
