// Issue #305 / A2 I-002: DAC cross-layer reference adapter core (leaf module).
// Portable: no Node built-ins, no engine internals, no observation/control
// imports, no DAC product-implementation dependency. Composition happens only
// in the public barrels (exports only).
export * from './contracts.js';
export * from './guards.js';
