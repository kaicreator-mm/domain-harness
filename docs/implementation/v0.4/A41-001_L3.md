# A41-001 L3 Evidence — DAC v0.0.4.1 successor reference/request-role foundation

**Version lane:** `v0.4`
**Task Issue:** #355 (trigger comment `5846236343`)
**Planning authority:** reviewed Task DAG #353; cumulative adoption audit #348 (comment `5836333294`); fresh review #349 (comment `5842794726`)
**Branch:** `task/v0.4-A41-001-dac-foundation` (JIT from exact live `v0.4`)
**Exact base:** `c2b43b3a1ff0d88a2f7a5220e270bc5635437bf8` / tree `2e5a62ac8f98b738609a3466293f8b6a8ad798ab`
**DAC v0.0.4.1 semantic freeze:** `75fee75b782ac229720dccd18d2a4ca54b285e51` / tree `c74cf5e3a0e6745da3eda6999836b61ee8103c60`
**PR base:** `v0.4`
**Review policy:** `review:required` (public API/schema surface; exact-SHA fresh independent review)
**L3:** REQUIRED

## 1. Frozen inputs

- `.dev-standard/VERSION` — ai-development-standard `3.4.0@418d244f23a6bf724acf5d4c4eff4ea292f1c4db`
- `.dev-standard/PROJECT_OVERRIDES.md`
- Issue #355 (concern, allowed write set, acceptance, required result shape)
- DAC v0.0.4.1 successor spec surfaces at the freeze commit:
  `spec/v0.0.4.1/FREEZE_MANIFEST.md`, `CROSS_LAYER_REFERENCES.md`,
  `LIFECYCLE_REFERENCE_REPAIRS.md`, `ASSEMBLY_CAPABILITY_EXCHANGE.md`,
  `AUTHORITY_DESIGNATION_DELEGATION.md`, `CONFORMANCE_MATRIX.md`
- Historical Harness release baseline `f2b61cc720a85d3e0fb94c1eed9b68f8ed855e6c`
  (DAC v0.0.3 conformance CONFORMANCE_PROVEN, version-bound, never relabeled)

## 2. Objective (concern boundary)

Establish the additive successor DAC v0.0.4.1 reference/request-role
foundation required by the reviewed DAG without rewriting or relabeling any
historical v0.0.3 adapter/evidence byte:

1. exact immutable DAC v0.0.4.1 semantic identity/pin representation
   (freeze commit AND tree) with deterministic fail-closed baseline
   verification;
2. successor request-vs-result role types for the two Harness-owned seam
   request roles the audit recorded as absent against C89/C108
   (`CompatibilityValidationRequestRef`, `RuntimeBindingRequestRef`) plus the
   universal §6 anti-alias chains and a fail-closed separation verifier;
3. deterministic capability-kind/currentness ordering primitives
   (C84/C85/C150-C153/C157/C158/C170/C171; ASSEMBLY_CAPABILITY_EXCHANGE §5 +
   §13) shared by later A41 consumers;
4. frozen predecessor baseline identities (v0.0.3, v0.0.4) retained for
   transition/adoption evidence only (F-06/C105) — never accepted as the
   successor pin and never upgraded into successor authority.

Explicit non-goals (authority ceiling): no AuthorityDesignation or
AuthorityAdoption issuance/verification (A41-002), no compatibility
evaluation/decision (A41-003), no composition-intake/selection/Manifest
verification (A41-004), no RuntimeBinding/RuntimeActivation behavior
(A41-005), no promotion/ApplicationSelection/Manifest/conformance authority,
no COMPATIBLE-producing function, no Runtime binding/activation
implementation beyond foundation contracts.

## 3. Allowed write set (as executed)

```text
packages/domain-harness/src/dac-v0041/**            (new: contracts.ts, guards.ts, index.ts)
packages/domain-harness/src/public-v4/index.ts      (new additive barrel)
packages/domain-harness/src/index.ts                (additive re-export of public-v4)
packages/domain-harness/package.json                (additive ./v4 subpath export)
packages/domain-harness/tests/dac-v0041-foundation/** (new focused suites)
packages/domain-harness/dist/**                     (mechanically regenerated build outputs)
docs/implementation/v0.4/A41-001_L3.md              (this evidence)
```

`.agent/execution/A41-001/**` was allowed but intentionally NOT materialized:
the repository's Execution Pack profile is disabled in
`.dev-standard/PROJECT_OVERRIDES.md` ("not materialized while disabled");
execution evidence lives in this document, the task Issue and the PR.

No other path was touched; in particular no `dac-v003*` byte, no
promotion/selection, composition-intake, runtime-binding/activation,
application-manifest, external-authority, public-v2/v3 or unrelated
public/runtime code changed.

## 4. L3 deliverables (Tests → Contract → Core Implementation → Failure Handling → Reference)

### 4.1 Tests (written first; 36 focused tests, 5 suites at the initial HEAD; 42 tests / 6 suites after the R1 repair — see §7)

- `dac-v0041-baseline-pin.test.ts` — exact pin values (commit+tree),
  frozen-at-runtime immutability, every baseline-drift shape fails closed
  (`UNSUPPORTED_DAC_BASELINE`, including both predecessor baselines and the
  v0.0.4.1 status-only descendant `07593ac…`), predecessor identities exact +
  evidence-only purpose, `predecessorOrigin` validation.
- `dac-v0041-request-result-separation.test.ts` — C89/C108 alias rejection
  and distinct-identity pass; frozen §6 chain table; advisory hints can
  never occupy the binding explicit target slot; request envelope carries no
  decision/result/outcome slot; forged carriers fail closed; mutable-alias
  identities rejected while advisory hints may stay mutable.
- `dac-v0041-capability-ordering.test.ts` — full deterministic matrix:
  descriptor establishment → capability kind (blocked, target not judged,
  even with stale inputs/unsupported target) → structural invalidity
  (FAIL_CLOSED dominates STALE) → staleness (STALE) → unsupported explicit
  target (INCOMPATIBLE) → evaluation phase; malformed facts fail closed;
  currentness-use mapping (C157); outcome polarity (C156/C158); classifier
  never yields COMPATIBLE and never assigns evaluation-phase outcome
  classes.
- `dac-v0041-authority-ceiling.test.ts` — frozen runtime-surface inventory;
  no issuance/verification authority names; the two request roles are
  reserved to dedicated constructors; authority-role adoption is
  identity-only; closed successor registry; cross-adapter carriers
  (v0.0.2/v0.0.3/v0.0.4.1) fail closed in both directions; additive reach
  into package root + `./v4`.
- `historical-v003-preservation.test.ts` — executable
  HISTORICAL_V003_PRESERVED proof: SHA-256 (LF-normalized committed-blob
  content) of all 50 `dac-v003*` files pinned to the exact base
  `c2b43b3a…`; historical adapter stays version-bound and distinct from the
  successor pin.

### 4.2 Contract (src/dac-v0041/contracts.ts)

`DAC_V0041_REFERENCE_ADAPTER_VERSION`, runtime-frozen `DAC_V0041_BASELINE`,
`DAC_V0041_PREDECESSOR_BASELINES` (+ evidence-only purpose constant),
combined successor role registry (predecessor roles imported unchanged +
v0.0.4/v0.0.4.1 additive roles incl. request roles, authority/result roles,
`authority-refusal`, `authority-adoption` as vocabulary only), the two
nominal request-role types, request/result alias chains, reference envelope
(+ `predecessorOrigin` evidence slot), disposition/outcome/currentness
vocabularies with outcome polarity table, capability-exchange facts and
classification types, error code union and `DacV0041ReferenceError`.

### 4.3 Core implementation (src/dac-v0041/guards.ts)

Private WeakSet minting registry (forged carriers fail closed; separate from
every historical adapter's registry); baseline equality gate that rejects
predecessor/status-only/drifted baselines; generic identity-only registry
adoption (the two foundation request roles reserved to their dedicated
constructors); `mintCompatibilityValidationRequestRef` /
`mintRuntimeBindingRequestRef` enforcing the request-envelope minimums and
the §4.1/§4.2 binding-explicit-target/advisory-hint split;
`verifyDacV0041RequestResultSeparation` (§6 anti-alias verifier);
`classifyDacV0041CapabilityExchange` (pure deterministic §5+§13 ordering);
`classifyDacV0041CurrentnessUse` (C157); `dacV0041OutcomeProducesResult`
(C156/C158 polarity).

### 4.4 Failure handling

Every decision path fails closed: `UNSUPPORTED_DAC_BASELINE`,
`INVALID_PREDECESSOR_ORIGIN`, `INVALID_REFERENCE`, `MUTABLE_ALIAS_REJECTED`,
`ROLE_MISMATCH`, `REQUEST_RESULT_ALIAS`, `ADVISORY_HINT_NOT_BINDING_TARGET`,
`INVALID_FACTS`, `INVALID_CURRENTNESS_STATE`. Unknown roles/states/outcomes
and malformed facts are rejected rather than guessed; the classifiers are
total over their closed vocabularies and pure (no hidden state — asserted by
the doubled-grid determinism test).

### 4.5 Reference (public surface + dist)

`src/public-v4/index.ts` (additive composition-only barrel),
`src/index.ts` additive re-export, `package.json` additive `./v4` subpath,
mechanically regenerated `dist/**` including `dist/dac-v0041/**` and
`dist/public-v4/**` (byte-identity guarded by the repository build gate).

## 5. Validation evidence

Clean Node Build Host gate (Windows Build Host, Node ≥22, exact final HEAD
of this task branch; full command transcript and exact SHA recorded in the
`A41_001_BUILD_HOST_RESULT` comment on #355 and on the PR):

```text
npm ci                     → PASS (frozen install)
npm run build              → PASS (incl. committed-dist byte identity)
npm run typecheck          → PASS
focused dac-v0041 suites   → PASS (36 tests)
npm test (full regression) → PASS
npm run lint               → PASS
pinned-standard/profile    → PASS (3.4.0@418d244f; overrides honored; no execution pack materialized)
DAC pin/currentness        → PASS (exact freeze commit+tree assertions)
write-set/diff check       → PASS (only the authorized paths changed)
clean tracked tree         → PASS
```

## 6. Boundaries left to successor tasks

- A41-002: designation/delegation/currentness chain verifier + AuthorityAdoption verifier.
- A41-003: successor compatibility request/validation/result closure (F-05 two-view).
- A41-004: composition-intake identity/selection/Manifest boundary verification.
- A41-005: Runtime binding/activation request-result closure and SoD.
- A41-006: cumulative C78–C172 conformance/adversarial/positive-boundary closure.

This task does not merge itself, does not self-review, and does not start
A41-002/#356.

## 7. R1 repair (Issue #363; failed fresh review #362)

Trigger: #363 comment `5847250812`. Blocking findings from the fresh
independent review of failed HEAD `9d3152fe…` (#362 comment `5847025507`):

- **P1-1 transitive immutability / predecessor-origin closure.** The mint/adopt
  core now defensively snapshots and freezes EVERY nested container
  participating in the exact request/evidence closure — `locatorHints`,
  `materialInputRefs`, `advisoryTargetHints`, the `opaque` record and the
  `predecessorOrigin` evidence record — matching the closure rule of the
  preserved historical v0.0.3 adoption core. `predecessorOrigin` is closed as
  a fresh frozen snapshot of the matched frozen table entry (canonical
  evidence-only purpose included, extra carrier fields dropped); the
  caller-owned carrier object is never aliased, so a caller mutating its
  carrier after mint can never change the exact historic-origin evidence the
  reference carries. The predecessor evidence record is validated completely:
  a retained `purpose` must be exactly `transition-or-adoption-evidence-only`
  — a predecessor identity relabeled toward any other purpose fails closed
  (`INVALID_PREDECESSOR_ORIGIN`). Post-mint mutation negatives added in
  `tests/dac-v0041-foundation/dac-v0041-immutability-closure.test.ts`
  (5 tests): caller-side mutation of every owned container after mint leaves
  the minted reference invariant and still WeakSet-minted; direct mutation
  attempts throw in strict mode; malformed origin carriers (relabeled /
  near-miss / empty / non-string purpose, drifted tree, array carrier) fail
  closed.
- **P2-1 malformed capability facts fail closed.** The runtime facts guard now
  proves the requested capability kind and every offered kind entry is a
  non-empty (non-whitespace-only) string. Empty/non-string kinds yield
  `INVALID_FACTS` — never a normal `blocked/missing-capability` or
  `evaluation` classification — while a legally empty offered list stays the
  normal Step-1 blocked classification. Hostile negatives added to
  `dac-v0041-capability-ordering.test.ts` (1 test), including the review's
  hostile pair (`requestedCapabilityKind: ""` + offered `[""]`).

Focused dac-v0041 suites after the repair: 42/42 (36 prior + 6 new). No
authority, role-separation, public-surface, DAC-pin or
historical-preservation semantics changed; the cumulative write set stays
inside the #355 authorization (33 files: the original 32 plus the new
`dac-v0041-immutability-closure.test.ts`). The complete clean Build Host gate
was re-run on the final exact successor HEAD; exact BASE/HEAD/TREE and the
command transcript are recorded in the `A41_001_R1_BUILD_HOST_RESULT` comment
on #363 and mirrored on PR #361.
